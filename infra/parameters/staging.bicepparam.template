using './main.bicep'

param projectName = '{{PROJECT}}'
param environment = 'staging'
param location = 'westeurope'
param organizationPrefix = '{{ORG}}'

param appServicePlanSku = { name: 'S1', tier: 'Standard', capacity: 1 }

param tags = {
  Environment: 'Staging'
  Project: '{{PROJECT}}'
  Organization: '{{ORG}}'
}
